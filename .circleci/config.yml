version: 2.1
working_directory: ~/repo
jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run: npm install
      - run: npm build
  test:
    docker:
      -image: circleci/node:14
    steps:
      - checkout
      - run: npm install
      - run: npm test


